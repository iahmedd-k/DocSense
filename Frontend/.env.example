# DocSense Frontend Environment Variables

# Base URL for Backend API (leave empty if using Vite proxy / relative path / same domain)
# Example for production: https://docsense-api.yourdomain.com
VITE_API_BASE_URL=
