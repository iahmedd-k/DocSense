services:

  api:
    build: .
    env_file: .env
    environment:
      DATABASE_URL: postgresql+psycopg://docsense:adminadmin@db:5432/docsense
    ports:
      - "8000:8000"
    depends_on:
      - db

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: docsense
      POSTGRES_PASSWORD: adminadmin
      POSTGRES_DB: docsense
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U docsense -d docsense"]
      interval: 5s
      timeout: 5s
      retries: 5